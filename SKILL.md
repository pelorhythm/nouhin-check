---
name: nouhin-check
description: 書き出した動画を納品前に実測チェックする。尺・解像度・fps・コーデック・ラウドネス(LUFS)・トゥルーピーク・黒フレーム・無音を一括で測り、基準から外れているものだけを指摘する。「納品チェック」「書き出しの確認」「ラウドネス測って」で発動。
disable-model-invocation: true
---

# 納品前チェック

書き出した動画を **人の耳と目で確認する前に、機械で測れるところを全部潰す**。
尺のズレ、ラウドネス違反、途中の黒フレーム——出してから気づくと事故になる項目を先に拾う。

---

## 使い方

### D&D: 書き出し前チェック（.prproj をドロップ）

Premiereのプロジェクトファイルをそのままドロップすると、**タイムライン構造の事故検出**になる。
シーケンスを選ぶ→解析→シーケンスタイムコード付きのリストで表示（HTML+テキスト自動保存）。

検出項目: 黒パカ疑い（全Vトラックの1〜2F隙間。調整レイヤーは覆いに数えない）／
カット点ズレ（トラック間で±1〜2Fずれた編集点＝テロップのこぼれ・抜け）／
トラック内の隠れた隙間／無効化クリップの取り残し／長い空白（情報）。

```bash
python3 ~/.claude/skills/nouhin-check/scripts/precheck.py <prproj> --list          # シーケンス一覧
python3 ~/.claude/skills/nouhin-check/scripts/precheck.py <prproj> --sequence 4    # 解析
```

※ .prprojはAdobeの非公開仕様。Premiereのメジャーアップデート後に動かなくなったら構造変更を疑う。
※ DaVinci案件は書き出し後チェックのみ対応。

### D&D: 書き出し後チェック — Premiere / DaVinci の書き出し後にすぐ使う

- **Mac**: デスクトップの **納品チェック.app** に書き出したファイルをドロップ
  （再作成は `bash ~/.claude/skills/nouhin-check/scripts/make_mac_droplet.sh`）
- **Windows**: `scripts/nouhin-check.bat` のショートカットをデスクトップに置き、そこへドロップ
  （要 Python 3 と ffmpeg。ffmpeg 未導入なら `winget install Gyan.FFmpeg`）

ドロップ後に納品先をドロップダウンで選ぶ（YouTube/SNS・TV放送・Web広告）→ 指定尺を任意入力 →
HTMLレポートがブラウザで開き、ファイルと同じフォルダに `YYYYMMDD_HHMM_納品チェック.html` として残る（納品記録に使える）。

D&D版は測定と✓/✗判定まで。ズレていた時の直し方は下の「結果の読み方」をClaude上で相談する。

### コマンドライン（Claude上での実行）

```bash
python3 ~/.claude/skills/nouhin-check/scripts/check.py <ファイル> [--target youtube|broadcast|web] [--duration 秒]
```

| オプション | 意味 |
|---|---|
| `--target youtube` | YouTube / SNS 向け（**既定**）。-14 LUFS ±1 |
| `--target broadcast` | 日本のTV放送・CM納品。-24.0 LKFS ±1（ARIB TR-B32） |
| `--target web` | Web広告 汎用。-16 LUFS ±1 |
| `--duration 30` | 指定尺。実測との差が0.1秒を超えたら指摘する |

複数ファイルをまとめて渡せる（連番の書き出しチェックに使う）。

```bash
python3 ~/.claude/skills/nouhin-check/scripts/check.py ~/Desktop/案件/40_書き出し/*.mp4 --target broadcast --duration 30
```

---

## 実行の前に確認すること

ユーザーが納品先を言っていない場合、**`--target` を勝手に決めない。** 必ず聞く。
-14 と -24 では10 LU も違うので、間違えると納品先で弾かれるか、音が明らかに小さい/大きい状態で出る。

尺指定がある案件（CM・広告枠）では `--duration` を必ず付ける。15秒/30秒枠は0.1秒のズレも許されない。

---

## 測っている項目

| 項目 | 判定 |
|---|---|
| 解像度 / fps / コーデック / pix_fmt / ビットレート | 表示のみ（入稿規定との照合は人が行う） |
| 尺 | `--duration` 指定時、差が0.1秒超で ✗ |
| 統合ラウドネス | 目標 ±1 LU を外れたら ✗ |
| トゥルーピーク | -1.0 dBTP 超で ✗（圧縮後の歪み防止） |
| ラウドネスレンジ | 表示のみ |
| 無音区間 | -50dB以下が0.5秒以上。頭に出たら ✗（頭切れの疑い） |
| 黒フレーム | 途中に出たら ✗（カット抜け・レンダリング事故の疑い） |

黒フレーム・無音が **頭と尻だけ** に出るのは正常なことが多い（フェード、余白）。
スクリプトは「途中」だけを問題として立てる。頭尻は情報として出すので、意図通りか人が見る。

---

## 結果の読み方（ここが判断層）

スクリプトは事実しか返さない。**ズレていた時に何をするかは、ここで判断する。**

### ラウドネスが外れている
- 目標より小さい → 上げる。ただし上げるとトゥルーピークが -1.0 に当たることがある。
  当たるならリミッターではなく、まず **素材のバランス（BGMを下げてナレーションを立てる）** を疑う
- 目標より大きい → 下げる。YouTubeは自動で下げられるだけなので実害は小さいが、
  **放送・CM納品では -24.0 ±1 は絶対**。1.1 LU 外れているだけでも直す

### トゥルーピーク超過
書き出し設定の問題であることが多い。マスターに -1.0 dBTP のトゥルーピークリミッターを入れて再書き出し。

### 途中の黒フレーム
ほぼ確実に事故。カットの繋ぎ抜け、レンダリング中断、素材の欠落。**該当秒数をタイムラインで直接確認する。**

### 尺のズレ
枠モノは尺が命。テールのフレーム調整で合わせる。フェードアウトを削って合わせると不自然になるので、
どこで削るかはユーザーに確認する。

---

## 関連

- ラウドネス基準の出どころ → `~/.claude/skills/Video_Skills/audio-and-music.md`
- CMコード・素材名の命名 → Obsidian `30_型ライブラリ/ノウハウ/CM素材ファイル命名規則.md`
- 媒体ごとの入稿規定 → Obsidian `入稿規定チェックリスト.md`

---

## やらないこと

- 基準外の値を勝手に直さない（測って指摘するだけ。再書き出しはユーザーが判断）
- 映像の内容（誤字・演出・尺の妥当性）は見ない → テロップは `jp-telop-proofer`、
  構成は `shot-variety-checker`
- 納品先ごとの独自基準は `check.py` に無い限り判定しない（基準の単一ソースは `check.py`）
- 元ファイルを上書き・変換・移動しない（読み取りと計測のみ）
